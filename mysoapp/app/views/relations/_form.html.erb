<%= form_for(relation) do |f| %>
  <% if relation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(relation.errors.count, "error") %> prohibited this relation from being saved:</h2>

      <ul>
      <% relation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user1_id %>
    <%= f.text_field :user1_id %>
  </div>

  <div class="field">
    <%= f.label :user2_id %>
    <%= f.text_field :user2_id %>
  </div>

  <!--div class="field">
    <%= f.label :status %>
    <%= f.text_field :status %>
  </div-->

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
